# 환경 변수 설정 가이드

## 🔧 에러 해결: "Cannot read properties of undefined (reading 'VITE_KAKAO_JS_KEY')"

이 에러는 환경 변수가 제대로 설정되지 않았을 때 발생합니다.

## ✅ 해결 방법

### 1. `.env` 파일 확인
프로젝트 루트에 `.env` 파일이 있는지 확인하세요:

```bash
# .env 파일 내용
VITE_KAKAO_JS_KEY=94e86b9b6ddf71039ab09c9902d2d79f
```

> **참고**: Kakao REST API 키는 서버 측에서 자동으로 관리됩니다 (`KAKAO_REST_API_KEY`).

### 2. 개발 서버 재시작 (중요!)
환경 변수를 추가하거나 수정한 후에는 **반드시 개발 서버를 재시작**해야 합니다:

```bash
# 1. 현재 서버 중지
Ctrl + C (또는 Cmd + C on Mac)

# 2. 서버 재시작
npm run dev
```

### 3. 브라우저 캐시 삭제
- 하드 리프레시: `Ctrl + Shift + R` (Windows/Linux) 또는 `Cmd + Shift + R` (Mac)
- 또는 브라우저 개발자 도구(F12) > Application > Clear storage

## 📁 파일 구조

```
프로젝트/
├── .env                    # 환경 변수 (gitignore에 포함)
├── .env.example            # 환경 변수 예시
├── vite-env.d.ts          # TypeScript 타입 정의
└── utils/
    └── kakao-config.ts     # 환경 변수 유틸리티
```

## 🔐 환경 변수 사용 방법

### ❌ 잘못된 방법
```typescript
const key = import.meta.env.VITE_KAKAO_JS_KEY; // undefined 가능성
```

### ✅ 올바른 방법
```typescript
import { getKakaoJsKey } from "../utils/kakao-config";

const key = getKakaoJsKey(); // 안전하게 가져오기
if (!key) {
  console.error("API key not configured");
  return;
}
```

## 🚨 주의사항

1. **환경 변수는 반드시 `VITE_` 접두사로 시작**해야 클라이언트에서 접근 가능합니다
2. `.env` 파일 수정 후 **개발 서버 재시작** 필수
3. `.env` 파일은 **절대 Git에 커밋하지 마세요** (보안!)
4. 배포 시 호스팅 서비스에 환경 변수 설정 필요

## 🎯 체크리스트

- [ ] `.env` 파일이 프로젝트 루트에 존재
- [ ] `VITE_KAKAO_JS_KEY=your_key` 형식으로 작성
- [ ] 개발 서버 재시작
- [ ] 브라우저 캐시 삭제
- [ ] 콘솔에서 에러 확인

## 🔍 디버깅

개발자 도구 콘솔에서 확인:

```javascript
// 콘솔에 입력
console.log(import.meta.env.VITE_KAKAO_JS_KEY);
// "94e86b9b6ddf71039ab09c9902d2d79f" 출력되어야 함
// undefined가 출력되면 서버 재시작 필요
```

## 📝 참고

- Vite 환경 변수 문서: https://vitejs.dev/guide/env-and-mode.html
- 카카오 개발자 센터: https://developers.kakao.com/
