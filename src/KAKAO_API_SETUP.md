# 카카오 API 설정 가이드

## ⚠️ 중요: 도메인 등록 필수!

**카카오맵 API는 등록된 도메인에서만 작동합니다.**

만약 `window.kakao is undefined` 에러가 발생하면, 대부분 도메인이 등록되지 않아서입니다.

### 빠른 해결 방법:

1. [카카오 개발자 콘솔](https://developers.kakao.com/) 접속
2. 내 애플리케이션 선택
3. **플랫폼 설정** → **Web 플랫폼 등록/수정**
4. 다음 도메인들을 모두 추가:
   - 현재 개발 중인 도메인 (예: `https://your-app.example.com`)
   - `http://localhost` (로컬 개발용)
   - `http://localhost:3000` (특정 포트)
   - `http://127.0.0.1` (대체 로컬호스트)
5. 저장 후 브라우저 새로고침

---

# 카카오 API 설정 가이드

## 📋 필요한 카카오 API 키

이 프로젝트는 **2가지 카카오 API 키**가 필요합니다:

### 1. 카카오 JavaScript Key (프론트엔드)
- **용도**: 지도 표시 (KakaoMap 컴포넌트)
- **사용 위치**: 프론트엔드 (브라우저)
- **환경 변수**: `VITE_KAKAO_JS_KEY`

### 2. 카카오 REST API Key (백엔드)
- **용도**: 장소 검색 API
- **사용 위치**: Supabase Edge Function
- **환경 변수**: `KAKAO_REST_API_KEY`

---

## 🔑 카카오 API 키 발급 방법

### Step 1: 카카오 개발자 계정 생성
1. [카카오 Developers](https://developers.kakao.com/) 접속
2. 카카오 계정으로 로그인
3. 우측 상단 "내 애플리케이션" 클릭

### Step 2: 애플리케이션 등록
1. "애플리케이션 추가하기" 클릭
2. 앱 이름 입력 (예: "여행 어플")
3. 사업자명 입력 (개인은 이름)
4. "저장" 클릭

### Step 3: 플랫폼 추가 (JavaScript Key용)
1. 생성한 앱 선택
2. 좌측 메뉴 "앱 설정" > "플랫폼" 클릭
3. "Web 플랫폼 추가" 클릭
4. 사이트 도메인 입력:
   - 로컬 개발: `http://localhost:5173`
   - Vercel 배포: `https://your-app.vercel.app`
5. "저장" 클릭

### Step 4: API 키 확인
1. 좌측 메뉴 "앱 설정" > "앱 키" 클릭
2. 다음 키들을 확인:
   - **JavaScript 키** → 프론트엔드용
   - **REST API 키** → 백엔드용

### Step 5: 카카오맵 API 활성화
1. 좌측 메뉴 "제품 설정" > "카카오맵" 클릭
2. "활성화" 버튼 클릭
3. 로컬 API, 키워드로 장소 검색하기 활성화 확인

---

## ⚙️ 환경 변수 설정

### 프론트엔드 (로컬 개발)

프로젝트 루트에 `.env` 파일 생성:

```bash
# .env
VITE_KAKAO_JS_KEY=your_javascript_key_here
```

### 백엔드 (Supabase Edge Function)

#### 방법 1: Supabase CLI 사용
```bash
supabase secrets set KAKAO_REST_API_KEY=your_rest_api_key_here
```

#### 방법 2: Supabase Dashboard 사용
1. [Supabase Dashboard](https://supabase.com/dashboard) 접속
2. 프로젝트 선택
3. 좌측 메뉴 "Edge Functions" 클릭
4. "Secrets" 탭 선택
5. "New Secret" 클릭
6. Name: `KAKAO_REST_API_KEY`
7. Value: REST API 키 입력
8. "Save" 클릭

---

## 🧪 테스트

### JavaScript Key 테스트
1. 앱 실행: `npm run dev`
2. 지역 선택 후 "지도에서 보기" 클릭
3. 지도가 정상적으로 표시되는지 확인

### REST API Key 테스트
1. 앱에서 장소 검색 기능 사용
2. 브라우저 콘솔에서 에러가 없는지 확인
3. 검색 결과가 정상적으로 표시되는지 확인

---

## ⚠️ 주의사항

### 1. 키 보안
- JavaScript 키는 브라우저에 노출되므로 **도메인 제한** 설정 필수
- REST API 키는 절대 프론트엔드에 노출하지 말 것

### 2. 도메인 설정
JavaScript 키는 등록된 도메인에서만 작동합니다:
- 개발: `http://localhost:5173`
- 배포: 실제 배포 URL 추가 필요

### 3. 할당량
- 카카오맵 API는 일일 사용량 제한이 있습니다
- 무료 플랜: 하루 100,000건
- 초과 시 유료 플랜 전환 필요

---

## 🐛 문제 해결

### 지도가 표시되지 않는 경우

**증상**: 지도 영역이 비어있거나 에러 메시지

**해결 방법**:
1. 브라우저 콘솔 확인
2. `VITE_KAKAO_JS_KEY` 환경 변수 설정 확인
3. 카카오 Developers에서 Web 플랫폼 도메인 확인
4. 카카오맵 API 활성화 확인
5. 개발 서버 재시작: `npm run dev`

### 장소 검색이 작동하지 않는 경우

**증상**: 검색 결과가 없거나 에러

**해결 방법**:
1. Supabase Edge Function 로그 확인
2. `KAKAO_REST_API_KEY` 환경 변수 설정 확인
3. Edge Function 재배포:
   ```bash
   supabase functions deploy server
   ```

### "Invalid appkey" 에러

**원인**: API 키가 잘못되었거나 도메인이 등록되지 않음

**해결 방법**:
1. API 키 재확인 (복사 시 공백 주의)
2. 카카오 Developers에서 도메인 재확인
3. 환경 변수 재설정
4. 브라우저 캐시 삭제 후 재시작

---

## 📚 참고 자료

- [카카오맵 JavaScript API 가이드](https://apis.map.kakao.com/web/guide/)
- [카카오 Local API 가이드](https://developers.kakao.com/docs/latest/ko/local/dev-guide)
- [카카오 Developers](https://developers.kakao.com/)

---

## 💡 요약

```bash
# 1. 카카오 Developers에서 앱 생성
# 2. Web 플랫폼 추가 (도메인 등록)
# 3. 카카오맵 API 활성화
# 4. API 키 발급 (JavaScript Key + REST API Key)

# 5. 프론트엔드 환경 변수 설정
echo "VITE_KAKAO_JS_KEY=your_javascript_key" > .env

# 6. 백엔드 환경 변수 설정
supabase secrets set KAKAO_REST_API_KEY=your_rest_api_key

# 7. 개발 서버 실행
npm run dev
```

완료! 🎉
